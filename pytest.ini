[pytest]
testpaths = tests
markers =
    P0: critical — must pass for submission
    P1: should pass — 14/18 minimum
    P2: nice to have
    e2e: end-to-end demo scenarios
    chaos: destructive tests
    performance: timing benchmarks
asyncio_mode = auto
